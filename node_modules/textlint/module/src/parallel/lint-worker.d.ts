import { Config } from "../config/config";
import type { TextlintFixResult, TextlintResult } from "@textlint/types";
export interface LintWorkerData {
    files: string[];
    config: Config;
    type: "lint" | "fix";
}
export type LintWorkerResults<type extends LintWorkerData["type"]> = type extends "lint" ? TextlintResult[] : TextlintFixResult[];
//# sourceMappingURL=lint-worker.d.ts.map